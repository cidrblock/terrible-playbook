---
# L027: This role has no meta/main.yml

- name: Echo broken role task message
  ansible.builtin.command:
    cmd: echo "I am a broken role task"
  changed_when: false

- name: Do something risky in the role
  shell: rm -rf /tmp/old_data/*
  become: true
